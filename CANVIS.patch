diff -ru a/HANDOVER.md b/HANDOVER.md
--- a/HANDOVER.md	2026-08-05 12:51:12.262844733 +0000
+++ b/HANDOVER.md	2026-08-05 12:51:12.263980721 +0000
@@ -204,11 +204,48 @@
   un alumne que passa dels exercicis directes als problemes semblaria que
   empitjora;
   (d) la feina anterior al trimestre no compta: la línia de base és l'últim
-  codi anterior al període.
+  codi anterior al període;
+  (e) la línia de base és **acumulativa** —la unió de tot el que s'ha vist—,
+  no l'últim codi tot sol. Normalment és el mateix, perquè cada codi conté
+  l'anterior. Deixa de ser-ho si l'alumne perd les dades del navegador i torna
+  a començar: llavors el seu codi següent en porta menys, i amb una base no
+  acumulativa el que repetia es tornava a comptar i el volum sortia inflat.
+  L'analitzador detecta la caiguda i l'avisa.
+- **Les qualificacions no es recalculen a cada tecla.** Hi ha un botó
+  «Accepta i calcula», i mentre hi hagi canvis sense aplicar el botó es posa
+  ambre i ho diu. Posar una nota a algú no és una previsualització, i veure
+  les xifres ballar mentre s'escriu un any convida a mirar-se-les abans que
+  estiguin acabades. La capçalera del resultat repeteix sempre el període i
+  els objectius amb què s'han calculat: una nota sense això al costat no vol
+  dir res.
+- **Qui té codis al full però cap dins del període hi surt amb un zero.** No
+  haver fet res durant el trimestre és una nota, no una absència de dades. Si
+  això li passa a tothom, però, l'analitzador avisa que segurament les dates
+  estan malament i diu quin rang cobreixen els codis de debò.
 - **L'encert pesa poc a la nota, i és deliberat.** Si pesés molt, a l'alumne
   li sortiria a compte no obrir pistes i evitar els exercicis de nivell 3, que
   és el contrari del que busca la graduació per dificultat. Els pesos són
   editables i la pàgina ho adverteix.
+- **El format de la data del formulari es detecta sol.** Google exporta la
+  marca de temps amb el format del full, que pot ser d/m/Y o m/d/Y, i amb dies
+  de l'1 al 12 no es poden distingir: "8/5/2026" tant pot ser el 8 de maig com
+  el 5 d'agost. Es resol comparant-ho amb la data que el codi porta a dins
+  —entre generar-lo i enviar-lo passen minuts, no mesos— i es decideix per
+  majoria per a tot el full. L'analitzador diu quin format ha triat.
+- **L'empaquetat és sense pèrdua, per construcció.** Set exercicis de 6 estats
+  possibles caben en 4 caràcters base32 perquè $6^7=279{.}936 \le 32^4=1{.}048{.}576$:
+  el canvi de base és injectiu i s'ha comprovat exhaustivament sobre les
+  279.936 combinacions. La sobrecàrrega respecte del mínim teòric és del 10,5 %,
+  que ve de quantitzar en grups de 7; són bits que sobren, no que falten.
+  L'enter més gran que es manipula és 279.935, deu ordres de magnitud per sota
+  del límit segur de JavaScript.
+- **Sí que hi ha pèrdua a tres llocs, tots deliberats i acotats:** l'hora va a
+  resolució de 2 minuts, el compte de cada etiqueta d'error topa a 31, i només
+  hi van les 3 etiquetes més repetides.
+- **La data ocupa 3 caràcters des de la versió RC2.** A l'RC1 n'ocupava 2, i
+  això topava als 1024 dies: a partir del 20 de juny de 2028 tots els codis
+  haurien dit la mateixa data, en silenci. El lector accepta les dues
+  versions, de manera que els codis ja emesos segueixen valent.
 - **L'hora té resolució de 2 minuts.** És el que cabia en dos caràcters, i per
   a la comprovació de "quant ha trigat entre generar-lo i enviar-lo" (llindar
   de 30 min) la precisió al minut no aporta res.
diff -ru a/README.md b/README.md
--- a/README.md	2026-08-05 12:51:12.238152186 +0000
+++ b/README.md	2026-08-05 12:51:12.239403208 +0000
@@ -140,16 +140,21 @@
   enviar-lo. L'analitzador té un camp de **feina mínima demanada**: qui no hi
   arriba surt marcat i la seva nota es pinta apagada, perquè un 10 sobre un
   exercici no es pot comparar amb un 6,6 sobre quaranta.
-- **És acumulatiu.** Cada codi conté tot el que s'ha fet fins llavors, no
-  només el d'aquella estona. Si l'alumne s'oblida d'enviar-ne un, el següent
-  ja ho porta tot, i el professorat només ha de mirar l'últim de cadascú.
+- **És acumulatiu i no es reinicia mai.** Cada codi conté tot el que s'ha fet
+  fins llavors, no només el d'aquella estona; copiar-lo no posa cap comptador
+  a zero. Si l'alumne s'oblida d'enviar-ne un, el següent ja ho porta tot, i
+  el professorat només ha de mirar l'últim de cadascú. L'única cosa que fa
+  baixar el comptador és perdre les dades del navegador (esborrar-les, canviar
+  de dispositiu, finestra privada); l'analitzador ho detecta i ho avisa.
 - **Antifrau.** Acaba amb dos caràcters calculats sobre tots els altres, en
   l'esperit de la lletra del DNI però amb mòdul primer i pesos per posició:
   detecta *totes* les substitucions d'un caràcter i *totes* les transposicions
   de dos. La nota no viatja dins del codi, es deriva del detall en llegir-lo,
   de manera que no hi pot haver un codi on la nota i el detall es contradiguin.
 - **L'analitzador és un sol fitxer** i funciona obrint-lo des del disc, sense
-  servidor ni connexió. No envia res enlloc.
+  servidor ni connexió. No envia res enlloc. Hi ha un full de respostes
+  d'exemple a `exemple-respostes.csv` per veure'l funcionar; vegeu
+  `EXEMPLE-LLEGEIX-ME.md`.
 - **Progrés del trimestre.** Com que els codis són acumulatius, la diferència
   entre dos codis seguits d'un mateix alumne és exactament la feina feta
   entremig. D'aquí surt una qualificació que pesa la **constància** (setmanes
diff -ru a/analitzador-repas.html b/analitzador-repas.html
--- a/analitzador-repas.html	2026-08-05 12:51:12.243657948 +0000
+++ b/analitzador-repas.html	2026-08-05 12:51:12.244880416 +0000
@@ -73,6 +73,12 @@
 .n-mitja{background:var(--ambre-suau);color:#6B4200}
 .n-baixa{background:var(--vermell-suau);color:var(--vermell)}
 .ajuda li{margin-bottom:.45rem}
+.camp{border-top:1px solid var(--vora);padding:.7rem 0 .1rem}
+.camp>b{display:block;font:600 11px/1.6 var(--mono);letter-spacing:.05em;
+  text-transform:uppercase;color:var(--apagat);margin-bottom:.4rem}
+.btn.pendent{background:var(--ambre);border-color:var(--ambre)}
+.capçalera-res{background:var(--blau-suau);border:1px solid #CBDCEA;border-radius:10px;
+  padding:.6rem .8rem;font-size:13.5px;margin-bottom:1rem}
 </style>
 </head>
 <body>
@@ -101,7 +107,7 @@
         obrir un CSV. Cal que hi hagi una columna amb el codi; si n'hi ha de correu,
         marca temporal o grup, també les farà servir.
       </p>
-      <textarea id="entrada" placeholder="Marca temporal	Adreça electrònica	Grup	Codi&#10;17/02/2026 10:41:03	alumne@escola.cat	4tA	RC1AM-8599X-…"></textarea>
+      <textarea id="entrada" placeholder="Marca temporal	Adreça electrònica	Grup	Codi&#10;17/02/2026 10:41:03	alumne@escola.cat	4tA	RC2AM-8599X-…"></textarea>
       <div style="display:flex;gap:.5rem;flex-wrap:wrap;margin-top:.7rem;align-items:center">
         <button class="btn" id="btn-llegeix">Llegeix</button>
         <label class="btn buit" style="position:relative;overflow:hidden">
@@ -157,30 +163,45 @@
   <!-- ── PROGRÉS DEL TRIMESTRE ────────────────────────────────────────── -->
   <section id="v-progres" hidden>
     <div class="targeta">
-      <h3>Període i objectius</h3>
-      <p class="petit apagat" style="margin:0 0 .7rem">
+      <h3>Quin període vols avaluar?</h3>
+      <p class="petit apagat" style="margin:0 0 .9rem">
         Els codis són acumulatius, així que la diferència entre dos codis seguits
         és exactament la feina feta entremig. D'aquí surt tot el que hi ha a sota:
         constància, volum i si va millorant.
       </p>
-      <div class="filtres">
-        <label class="petit apagat">Del
-          <input type="date" id="p-ini"></label>
-        <label class="petit apagat">al
-          <input type="date" id="p-fi"></label>
-        <select id="p-preset">
-          <option value="">Trimestre…</option>
-          <option value="1">1r (set–des)</option>
-          <option value="2">2n (gen–març)</option>
-          <option value="3">3r (abril–juny)</option>
-          <option value="tot">Tot el que hi ha</option>
-        </select>
-        <label class="petit apagat">Setmanes esperades
-          <input type="number" id="p-setm" value="10" min="1" max="30" style="width:64px"></label>
-        <label class="petit apagat">Exercicis esperats
-          <input type="number" id="p-exerc" value="60" min="1" max="999" style="width:74px"></label>
-        <label class="petit apagat" title="Els de nivell 3 compten 1,3 i els de nivell 1, 0,8">
-          <input type="checkbox" id="p-dif" checked> pondera per dificultat</label>
+
+      <div class="camp">
+        <b>Període</b>
+        <div class="filtres" style="margin:0">
+          <label class="petit apagat">Del
+            <input type="date" id="p-ini"></label>
+          <label class="petit apagat">al
+            <input type="date" id="p-fi"></label>
+          <select id="p-preset">
+            <option value="">O tria'n un…</option>
+            <option value="1">1r trimestre (set–des)</option>
+            <option value="2">2n trimestre (gen–març)</option>
+            <option value="3">3r trimestre (abril–juny)</option>
+            <option value="tot">Tot el que hi ha al full</option>
+          </select>
+        </div>
+      </div>
+
+      <div class="camp">
+        <b>Quanta feina demanes en aquest període</b>
+        <div class="filtres" style="margin:0">
+          <label class="petit apagat">Setmanes amb feina
+            <input type="number" id="p-setm" value="10" min="1" max="40" style="width:64px"></label>
+          <label class="petit apagat">Exercicis en total
+            <input type="number" id="p-exerc" value="60" min="1" max="999" style="width:74px"></label>
+          <label class="petit apagat" title="Els de nivell 3 compten 1,3 i els de nivell 1, 0,8">
+            <input type="checkbox" id="p-dif" checked> pondera per dificultat</label>
+        </div>
+        <p class="petit apagat" style="margin:.35rem 0 0">
+          Qui arribi a les dues xifres té el màxim d'aquests dos apartats. Les
+          setmanes compten només si porten feina <b>nova</b>: reenviar el mateix
+          codi no suma.
+        </p>
       </div>
       <details style="margin-top:.4rem">
         <summary class="petit apagat" style="cursor:pointer">Pesos de la qualificació</summary>
@@ -203,6 +224,10 @@
           què estàs incentivant.
         </p>
       </details>
+      <div style="display:flex;gap:.6rem;align-items:center;flex-wrap:wrap;margin-top:1rem">
+        <button class="btn" id="p-accepta">Accepta i calcula</button>
+        <span class="petit" id="p-estat"></span>
+      </div>
     </div>
 
     <div id="p-resultats" style="margin-top:1rem"></div>
@@ -212,7 +237,7 @@
   <section id="v-un" hidden>
     <div class="targeta">
       <h3>Enganxa un codi</h3>
-      <input type="text" id="codi-un" placeholder="RC1AM-8599X-00853-…" style="width:100%;font-family:var(--mono)">
+      <input type="text" id="codi-un" placeholder="RC2AM-8599X-00853-…" style="width:100%;font-family:var(--mono)">
       <div style="margin-top:.7rem"><button class="btn" id="btn-un">Llegeix-lo</button></div>
     </div>
     <div id="un-resultat" style="margin-top:1rem"></div>
@@ -284,11 +309,11 @@
 
    ── FORMAT ────────────────────────────────────────────────────────────────
 
-     RC1 SSS DD HH MMM  [ per cada full: G + grups de 4 ]  [ DIAG ] EEEEEEEEE KK
+     RC2 SSS DDD HH MMM  [ per cada full: G + grups de 4 ]  [ DIAG ] EEEEEEEEE KK
 
-     RC1   3   marca i versió
+     RC2   3   marca i versió
      SSS   3   salt aleatori
-     DD    2   dia (dies des de l'1/9/2025)
+     DDD   3   dia (dies des de l'1/9/2025)
      HH    2   hora (minuts/2 des de mitjanit)
      MMM   3   màscara: bits 0-11 = fulls presents, bit 12 = hi ha diagnòstic
      G     1   nombre de grups d'aquest full
@@ -343,6 +368,14 @@
   var ALF = "0123456789ABCDEFGHJKMNPQRSTVWXYZ";
 
   var EPOCA = Date.UTC(2025, 8, 1);           /* 1 de setembre de 2025 */
+
+  /* La data ocupa 3 caràcters (32768 dies, fins al 2115). A la versió RC1
+     n'ocupava 2, i això topava als 1024 dies: a partir del 20 de juny de 2028
+     tots els codis haurien dit la mateixa data, en silenci i sense que res
+     avisés, i l'anàlisi per trimestres hauria quedat inservible. Costava un
+     caràcter arreglar-ho. El lector accepta les dues versions. */
+  var CAR_DIA = { RC1: 2, RC2: 3 };
+  var VERSIO = "RC2";
   var ESTATS = ["", "net", "segon", "pista", "fallat", "vist"];
   var PES = { net: 10, segon: 7, pista: 6, fallat: 0, vist: 0, "": 0 };
 
@@ -401,14 +434,14 @@
     for (var i = 0; i < 3; i++) salt += ALF.charAt(Math.floor(Math.random() * 32));
 
     var dia = Math.floor((Date.UTC(ara.getFullYear(), ara.getMonth(), ara.getDate()) - EPOCA) / 86400000);
-    dia = Math.max(0, Math.min(1023, dia));
+    dia = Math.max(0, Math.min(32767, dia));
     var hora = Math.min(719, Math.floor((ara.getHours() * 60 + ara.getMinutes()) / 2));
 
     var mask = 0;
     fulls.forEach(function (f) { mask |= (1 << (f.n - 1)); });
     if (diag) mask |= (1 << 12);
 
-    var cos = "RC1" + salt + enc(dia, 2) + enc(hora, 2) + enc(mask, 3);
+    var cos = VERSIO + salt + enc(dia, CAR_DIA[VERSIO]) + enc(hora, 2) + enc(mask, 3);
 
     fulls.forEach(function (f) {
       var codis = f.estats.map(function (e) { return Math.max(0, ESTATS.indexOf(e || "")); });
@@ -465,8 +498,9 @@
     var T = window.RE_TAULES;
     var s = neteja(raw);
     if (!s) return { ok: false, error: "Codi buit" };
-    if (s.slice(0, 3) !== "RC1") {
-      return { ok: false, error: "No sembla un codi de repàs-ESO (ha de començar per RC1)" };
+    var versio = s.slice(0, 3);
+    if (!CAR_DIA[versio]) {
+      return { ok: false, error: "No sembla un codi de repàs-ESO (ha de començar per RC2)" };
     }
     if (s.length < 15) return { ok: false, error: "Codi massa curt" };
 
@@ -477,7 +511,7 @@
 
     var p = 3;
     var salt = s.slice(p, p + 3); p += 3;
-    var dia = dec(s.slice(p, p + 2)); p += 2;
+    var dia = dec(s.slice(p, p + CAR_DIA[versio])); p += CAR_DIA[versio];
     var hora = dec(s.slice(p, p + 2)); p += 2;
     var mask = dec(s.slice(p, p + 3)); p += 3;
 
@@ -530,7 +564,7 @@
     }
 
     r = {
-      ok: true, integre: integre, salt: salt,
+      ok: true, integre: integre, salt: salt, versio: versio,
       data: data, hora: Math.floor(minuts / 60), minut: minuts % 60,
       fulls: fulls, diag: diag, errs: errs,
       error: integre ? null : "Els caràcters de control no quadren: el codi s'ha copiat malament o s'ha modificat"
@@ -627,7 +661,7 @@
       $("#v-full").hidden = b.dataset.mode !== "full";
       $("#v-progres").hidden = b.dataset.mode !== "progres";
       $("#v-un").hidden = b.dataset.mode !== "un";
-      if (b.dataset.mode === "progres") pintaProgres();
+      if (b.dataset.mode === "progres") { proposaPeriode(); pintaProgres(); }
       $("#v-ajuda").hidden = b.dataset.mode !== "ajuda";
     };
   });
@@ -649,7 +683,7 @@
      columna es digui de cap manera concreta: si les capçaleres no ajuden,
      es busca la columna que conté un codi. */
   function buscaCodi(text) {
-    var m = String(text || "").toUpperCase().match(/RC1[0-9A-Z\-\s]{10,}/);
+    var m = String(text || "").toUpperCase().match(/RC[12][0-9A-Z\-\s]{10,}/);
     return m ? m[0].trim() : null;
   }
 
@@ -693,9 +727,41 @@
     var m = String(ts).match(/(\d{1,2}):(\d{2})/);
     return m ? (+m[1]) * 60 + (+m[2]) : null;
   }
+  /* Google exporta la data amb el format del full, que pot ser d/m/Y (català)
+     o m/d/Y (anglès). Amb dies del 1 al 12 no es poden distingir mirant la
+     data tota sola: "8/5/2026" tant pot ser el 8 de maig com el 5 d'agost.
+
+     Sí que es poden distingir comparant-la amb la data que el codi porta a
+     dins, perquè entre generar-lo i enviar-lo passen minuts, no mesos. Es
+     decideix una vegada per a tot el full i per majoria, que és molt més
+     robust que fila a fila. */
+  var FORMAT_DATA = "dm";
+
+  function detectaFormatData(brut) {
+    var dm = 0, md = 0;
+    brut.forEach(function (r) {
+      var p = RE_CODI.llegeix(r.codi);
+      if (!p.ok) return;
+      var m = String(r.enviat).match(/(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})/);
+      if (!m) return;
+      var a = +m[1], b = +m[2], any = +m[3];
+      if (a > 12) { dm++; return; }              /* el primer no pot ser mes */
+      if (b > 12) { md++; return; }
+      var base = p.data.getTime();
+      var v1 = Math.abs(new Date(any, b - 1, a).getTime() - base);   /* d/m/Y */
+      var v2 = Math.abs(new Date(any, a - 1, b).getTime() - base);   /* m/d/Y */
+      if (v1 < v2) dm++; else if (v2 < v1) md++;
+    });
+    return md > dm ? "md" : "dm";
+  }
+
   function dataEnviament(ts) {
     var s = String(ts), m = s.match(/(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})/);
-    if (m) return m[3] + "-" + ("0" + m[2]).slice(-2) + "-" + ("0" + m[1]).slice(-2);
+    if (m) {
+      var dia = FORMAT_DATA === "md" ? m[2] : m[1];
+      var mes = FORMAT_DATA === "md" ? m[1] : m[2];
+      return m[3] + "-" + ("0" + mes).slice(-2) + "-" + ("0" + dia).slice(-2);
+    }
     m = s.match(/(\d{4})-(\d{2})-(\d{2})/);
     return m ? m[0] : "";
   }
@@ -812,6 +878,7 @@
       + dada("Amb pista", p.resum.comptes.pista)
       + dada("Al segon intent", p.resum.comptes.segon)
       + dada("Fallats", p.resum.comptes.fallat)
+      + dada("Oberts i deixats", p.resum.comptes.vist)
       + dada("Per nivell", "1r " + p.resum.perDif[1] + " · 2n " + p.resum.perDif[2] + " · 3r " + p.resum.perDif[3])
       + dada("Codi generat", r.dataGen + " " + ("0" + p.hora).slice(-2) + ":" + ("0" + p.minut).slice(-2))
       + "</div>";
@@ -985,9 +1052,10 @@
     var brut = llegeixFull(txt);
     if (!brut.length) {
       $("#estat-carrega").innerHTML = '<span class="mal">No hi he trobat cap codi. '
-        + "Els codis comencen per RC1.</span>";
+        + "Els codis comencen per RC2 (o RC1, els antics).</span>";
       return;
     }
+    FORMAT_DATA = detectaFormatData(brut);
     files = processa(brut);
     obert = null;
     var sel = $("#f-full");
@@ -996,11 +1064,16 @@
     vistos.sort(function (a, b) { return a - b; });
     sel.innerHTML = '<option value="">Tots els fulls</option>'
       + vistos.map(function (n) { return '<option value="' + n + '">Full ' + n + "</option>"; }).join("");
-    $("#estat-carrega").innerHTML = '<span class="ok">' + files.length + " codis llegits</span>";
+    $("#estat-carrega").innerHTML = '<span class="ok">' + files.length + " codis llegits</span>"
+      + ' <span class="apagat petit">· dates en format '
+      + (FORMAT_DATA === "md" ? "mes/dia/any" : "dia/mes/any") + "</span>";
     $("#resultats").hidden = false;
     obertP = null;
+    cfgAplicada = null;
+    $("#p-accepta").classList.remove("pendent");
+    $("#p-estat").textContent = "";
     pinta();
-    if (!$("#v-progres").hidden) pintaProgres();
+    if (!$("#v-progres").hidden) { proposaPeriode(); pintaProgres(); }
   }
 
   $("#btn-llegeix").onclick = function () { carrega($("#entrada").value); };
@@ -1017,9 +1090,26 @@
 
   ["#p-ini", "#p-fi", "#p-setm", "#p-exerc", "#p-dif",
    "#p-w1", "#p-w2", "#p-w3", "#p-w4"].forEach(function (s) {
-    $(s).oninput = function () { obertP = null; pintaProgres(); };
-    $(s).onchange = function () { obertP = null; pintaProgres(); };
+    $(s).oninput = marcaPendent;
+    $(s).onchange = marcaPendent;
   });
+  $("#p-accepta").onclick = aplicaProgres;
+
+  /* En obrir la pestanya per primera vegada, s'omplen les dates amb el que
+     cobreixen les dades. És una proposta, no una decisió: fins que no es prem
+     el botó no es calcula res. */
+  function proposaPeriode() {
+    if (cfgAplicada || $("#p-ini").value || !files.length) return;
+    var ds = files.filter(function (r) { return r.p.ok; }).map(function (r) { return quan(r.p); });
+    if (!ds.length) return;
+    var mn = new Date(Math.min.apply(null, ds)), mx = new Date(Math.max.apply(null, ds));
+    $("#p-ini").value = mn.toISOString().slice(0, 10);
+    $("#p-fi").value = mx.toISOString().slice(0, 10);
+    $("#p-setm").value = Math.max(1, Math.round((mx - mn) / 604800000) + 1);
+    $("#p-estat").innerHTML = '<span class="apagat">Hi ha codis del '
+      + mn.toISOString().slice(0, 10) + " al " + mx.toISOString().slice(0, 10)
+      + ". Ajusta el que calgui i prem <b>Accepta i calcula</b>.</span>";
+  }
 
   /* Els trimestres es dedueixen de l'any que tenen les dades, no d'un any
      fix: així el fitxer segueix servint el curs vinent. */
@@ -1041,7 +1131,11 @@
     } else if (v === "1") { posa((any - 1) + "-09-01", (any - 1) + "-12-22", 14); }
     else if (v === "2") { posa(any + "-01-08", any + "-03-31", 11); }
     else { posa(any + "-04-01", any + "-06-20", 11); }
-    obertP = null; pintaProgres();
+    marcaPendent();
+    if (!cfgAplicada) {
+      $("#p-estat").innerHTML = '<span class="apagat">Període posat. Ara prem '
+        + "<b>Accepta i calcula</b>.</span>";
+    }
   };
 
   $("#btn-csv").onclick = function () {
@@ -1178,17 +1272,31 @@
       });
       /* La feina d'abans del trimestre no compta: es parteix de l'últim codi
          anterior al període com a línia de base. */
-      var prev = abans.length ? abans[abans.length - 1].mapa : {};
+      var prev = {};
+      if (abans.length) Object.keys(abans[abans.length - 1].mapa).forEach(function (id) {
+        prev[id] = abans[abans.length - 1].mapa[id];
+      });
 
+      /* La línia de base és ACUMULATIVA: la unió de tot el que s'ha vist fins
+         ara, no l'últim codi tot sol. Normalment és el mateix, perquè cada
+         codi conté l'anterior. Deixa de ser-ho si l'alumne perd les dades del
+         navegador (les esborra, canvia de dispositiu, finestra privada) i
+         torna a començar: llavors el seu codi següent porta MENYS exercicis
+         que el previ. Amb una línia de base acumulativa, allò que repeteix no
+         es torna a comptar; amb l'últim codi tot sol, sí, i el volum del
+         trimestre sortia inflat. */
+      var perduda = [];
       var passos = [];
       dins.forEach(function (c) {
-        var nous = [], repetits = [];
+        var nous = [], repetits = [], vistos = Object.keys(c.mapa).length;
+        var previs = Object.keys(prev).length;
         Object.keys(c.mapa).forEach(function (id) {
           if (!prev[id]) nous.push(c.mapa[id]);
           else if (prev[id].e !== c.mapa[id].e) repetits.push({ e: c.mapa[id].e, d: c.mapa[id].d, abans: prev[id].e });
         });
-        passos.push({ quan: c.quan, nous: nous, repetits: repetits, codi: c.r, total: Object.keys(c.mapa).length });
-        prev = c.mapa;
+        if (previs && vistos < previs) perduda.push({ quan: c.quan, abans: previs, ara: vistos });
+        passos.push({ quan: c.quan, nous: nous, repetits: repetits, codi: c.r, total: vistos });
+        Object.keys(c.mapa).forEach(function (id) { prev[id] = c.mapa[id]; });
       });
 
       /* Un enviament només compta com a dia de feina si porta exercicis nous.
@@ -1251,7 +1359,7 @@
         dies: nDies, setmanes: nSetm, passos: ambFeina,
         exercicis: glob.n, volum: volum, glob: glob, A: A, B: B,
         dEncert: dEncert, dPistes: dPistes, mesurable: mesurable, repetits: repetits,
-        comp: comp, nota: nota,
+        comp: comp, nota: nota, perduda: perduda,
         primer: dins.length ? dins[0].quan : null,
         ultim: dins.length ? dins[dins.length - 1].quan : null
       };
@@ -1270,19 +1378,90 @@
       Math.round(100 * Math.min(1, v)) + "%" + (color ? ";background:" + color : "") + '"></i></span>';
   }
 
+  /* Les qualificacions NO es recalculen a cada tecla: es recalculen quan es
+     prem "Accepta i calcula". Posar una nota a algú no és una previsualització,
+     i veure les xifres ballar mentre s'escriu un any convida a mirar-se-les
+     abans que estiguin acabades. Mentre hi hagi canvis sense aplicar, el botó
+     es posa ambre i ho diu. */
+  var cfgAplicada = null;
+
+  function marcaPendent() {
+    if (!cfgAplicada) return;
+    $("#p-accepta").classList.add("pendent");
+    $("#p-estat").innerHTML = '<span class="avis">Has canviat alguna cosa: '
+      + "prem <b>Accepta i calcula</b> per tornar a fer els números.</span>";
+  }
+
+  function dataDeText(v) { return v ? new Date(v + "T00:00:00") : null; }
+
+  function aplicaProgres() {
+    if (!files.length) {
+      $("#p-estat").innerHTML = '<span class="mal">Primer has de llegir un full '
+        + "de respostes a la primera pestanya.</span>";
+      return;
+    }
+    var i = dataDeText($("#p-ini").value), f = dataDeText($("#p-fi").value);
+    if (!i || !f) {
+      $("#p-estat").innerHTML = '<span class="mal">Falta dir de quina data a '
+        + "quina. Pots triar un trimestre de la llista.</span>";
+      return;
+    }
+    if (i > f) {
+      $("#p-estat").innerHTML = '<span class="mal">La data d\'inici és posterior '
+        + "a la de final.</span>";
+      return;
+    }
+    cfgAplicada = cfgProgres();
+    $("#p-accepta").classList.remove("pendent");
+    $("#p-estat").textContent = "";
+    obertP = null;
+    pintaProgres();
+  }
+
   function pintaProgres() {
     if (!files.length) {
-      $("#p-resultats").innerHTML = '<div class="buit">Primer llegeix un full de respostes.</div>';
+      $("#p-resultats").innerHTML = '<div class="buit">Primer llegeix un full de respostes '
+        + "a la pestanya <b>Full de respostes</b>.</div>";
       return;
     }
-    var cfg = cfgProgres();
+    if (!cfgAplicada) {
+      $("#p-resultats").innerHTML = '<div class="buit">Digues quin període vols avaluar '
+        + "i quanta feina hi demanes, i prem <b>Accepta i calcula</b>.</div>";
+      return;
+    }
+    var cfg = cfgAplicada;
     var res = analitzaTrimestre(files, cfg);
     if (!res.length) {
-      $("#p-resultats").innerHTML = '<div class="buit">Cap alumne amb codis vàlids en aquest període.</div>';
+      $("#p-resultats").innerHTML = '<div class="buit">Cap codi vàlid al full.</div>';
+      return;
+    }
+    /* Qui té codis al full però cap dins del període hi surt igualment, amb
+       un zero: no haver fet res durant el trimestre és una nota, no una
+       absència de dades. Ara bé, si això li passa a TOTHOM, el més probable
+       no és que la classe hagi plegat sinó que les dates estiguin malament. */
+    if (res.every(function (a) { return !a.enviaments; })) {
+      var ds = files.filter(function (r) { return r.p.ok; }).map(function (r) { return quan(r.p); });
+      var mn = new Date(Math.min.apply(null, ds)), mx = new Date(Math.max.apply(null, ds));
+      $("#p-resultats").innerHTML = '<div class="buit">Cap alumne no ha enviat res '
+        + "entre aquestes dues dates.<br><span class=\"petit\">Els codis del full van "
+        + "del <b>" + mn.toISOString().slice(0, 10) + "</b> al <b>"
+        + mx.toISOString().slice(0, 10) + "</b>.</span></div>";
       return;
     }
 
-    var h = '<h2>Qualificació del període</h2>';
+    /* Que sempre quedi escrit a quines condicions correspon el que es veu:
+       una nota sense el període i els objectius al costat no vol dir res. */
+    var dt = function (d) {
+      return ("0" + d.getDate()).slice(-2) + "/" + ("0" + (d.getMonth() + 1)).slice(-2)
+        + "/" + d.getFullYear();
+    };
+    var h = '<div class="capçalera-res"><b>' + dt(cfg.ini) + " – " + dt(cfg.fi) + "</b>"
+      + '<span class="apagat"> · es demanen </span><b>' + cfg.setmanes + " setmanes</b>"
+      + '<span class="apagat"> amb feina i </span><b>' + cfg.exercicis + " exercicis</b>"
+      + (cfg.difPes ? '<span class="apagat">, ponderats per dificultat</span>' : "")
+      + '<span class="apagat"> · pesos ' + cfg.pesos.join("/") + "</span></div>";
+
+    h += '<h2>Qualificació del període</h2>';
     h += '<div class="taula-env"><table id="t-progres"><thead><tr>'
       + "<th>Alumne</th><th>Grup</th><th>Dies</th><th>Setmanes</th><th>Exercicis</th>"
       + "<th>Encert</th><th>Pistes</th><th>Millora</th><th>Nota</th><th></th>"
@@ -1290,8 +1469,14 @@
 
     res.forEach(function (a, i) {
       h += '<tr class="clic" data-p="' + i + '">'
-        + "<td>" + a.alumne + '</td><td class="apagat">' + (a.grup || "—") + "</td>"
-        + "<td>" + a.dies + (a.enviaments > a.ambFeina
+        + "<td>" + a.alumne + (a.enviaments ? "" :
+            ' <span class="xip" title="Té codis al full, però cap en aquest període">'
+            + "res en aquest període</span>") + "</td>"
+        + '<td class="apagat">' + (a.grup || "—") + "</td>"
+        + "<td>" + (a.perduda.length
+            ? '<span class="marca avis" title="En algun moment el seu codi va portar menys '
+              + 'exercicis que abans: sembla que va perdre les dades del navegador">!</span> ' : "")
+        + a.dies + (a.enviaments > a.ambFeina
             ? ' <span class="apagat petit" title="' + (a.enviaments - a.ambFeina)
               + ' enviament(s) sense feina nova">(+' + (a.enviaments - a.ambFeina) + ")</span>" : "") + "</td>"
         + "<td>" + a.setmanes + '<span class="apagat"> / ' + cfg.setmanes + "</span> "
@@ -1328,6 +1513,14 @@
       + dada("Constància fluixa", pocaConst + " de " + res.length)
       + "</div>";
 
+    var perduts = res.filter(function (a) { return a.perduda.length; });
+    if (perduts.length) {
+      h += '<p class="petit" style="margin-top:.7rem"><span class="avis marca">!</span> '
+        + "<b>Han perdut el progrés del navegador</b> en algun moment: "
+        + perduts.map(function (a) { return a.alumne; }).join(", ")
+        + ". Convé recordar-los que facin servir sempre el mateix navegador.</p>";
+    }
+
     if (concentrat.length) {
       h += '<p class="petit" style="margin-top:.7rem"><span class="avis marca">!</span> '
         + "<b>Feina concentrada en pocs dies</b> (molt volum, poca constància): "
@@ -1408,6 +1601,19 @@
     });
     h += "</table></div>";
 
+    if (a.perduda.length) {
+      h += '<p class="petit" style="margin-top:.6rem"><span class="marca avis">!</span> '
+        + "<b>Sembla que ha perdut el progrés del navegador.</b> ";
+      a.perduda.forEach(function (x) {
+        h += "El " + x.quan.toISOString().slice(0, 10) + " el seu codi va passar de portar "
+          + x.abans + " exercicis a portar-ne " + x.ara + ". ";
+      });
+      h += "El progrés es desa al navegador: esborrar-ne les dades, canviar de "
+        + "dispositiu o treballar en una finestra privada el fa començar de zero. "
+        + "El que ja tenia comptat no s'ha perdut (els codis anteriors el porten), "
+        + "i el que ha repetit no s'ha tornat a comptar.</p>";
+    }
+
     if (a.enviaments > a.ambFeina) {
       h += '<p class="petit apagat" style="margin-top:.6rem">Hi ha ' + (a.enviaments - a.ambFeina)
         + " enviament(s) que no portaven cap exercici nou: no compten com a dia de feina.</p>";
diff -ru a/exemple-respostes.csv b/exemple-respostes.csv
--- a/exemple-respostes.csv	2026-08-05 12:51:12.254913290 +0000
+++ b/exemple-respostes.csv	2026-08-05 12:51:12.258878382 +0000
@@ -0,0 +1,27 @@
+﻿Marca de temps;Adreça electrònica;Selecciona la classe;Enganxa aquí el codi que has copiat a step-quiz
+9/22/2026 18:40:28;aina.puig@insterrassa.cat;1r BAT A;RC2JH-X0C2H-G4011-70BR6-JD9C1-5VFP4-C3ZZ0-ZZ0S8
+9/24/2026 16:20:26;claudia.moya@insterrassa.cat;1r BAT B;RC232-G0C4F-A0012-716D4-8P04C-42H2Z-Z0TN
+9/25/2026 15:30:05;dani.serra@insterrassa.cat;1r BAT B;RC263-Z0C5E-H0011-4HW64-C2ZZ0-ZZ07J
+9/29/2026 19:10:48;aina.puig@insterrassa.cat;1r BAT A;RC2PM-P0C9H-Z4012-70CM4-PH86J-D9C15-VFP4C-4312Z-Z0P4
+10/2/2026 12:10:17;bernat.rius@insterrassa.cat;1r BAT A;RC29M-E0CCB-D0011-1RH04-C2ZZ0-ZZ0E5
+10/6/2026 18:55:33;aina.puig@insterrassa.cat;1r BAT A;RC2D8-90CGH-Q4013-70CM4-PHY67-406JD-9C15V-FP4C5-4Z2ZZ-0KE
+10/8/2026 15:40:30;dani.serra@insterrassa.cat;1r BAT B;RC263-Z0C5E-H0011-4HW64-C2ZZ0-ZZ07J
+10/9/2026 17:05:47;claudia.moya@insterrassa.cat;1r BAT B;RC23V-Z0CKG-00012-716D5-MN12H-44C5Z-Z0DV
+10/14/2026 20:05:43;aina.puig@insterrassa.cat;1r BAT A;RC2JG-S0CRJ-T4014-70CM4-PHY67-6R3M3-W6JD9-C15VF-P4Z44-C5ZZ0-R4
+10/21/2026 17:30:44;aina.puig@insterrassa.cat;1r BAT A;RC2DB-N0CZG-D4015-70CM4-PHY67-6R3M4-33NZG-6JD9C-15VFP-4Z52R-3ZZ09-B
+10/22/2026 14:55:17;dani.serra@insterrassa.cat;1r BAT B;RC263-Z0C5E-H0011-4HW64-C2ZZ0-ZZ07J
+10/28/2026 19:25:48;aina.puig@insterrassa.cat;1r BAT A;RC2PS-30D6J-64016-70CM4-PHY67-6R3M4-33NZQ-1PQT6-JD9C1-5VFP2-R44Z5-ZZ0EX
+10/30/2026 19:55:28;claudia.moya@insterrassa.cat;1r BAT B;RC2H4-W0D8F-P0053-716D5-MN171-6R14Q-W02H5-4Z3ZZ-027
+11/4/2026 18:15:30;aina.puig@insterrassa.cat;1r BAT A;RC2HH-B0DDH-34037-70CM4-PHY67-6R3M4-33NZQ-1PQV1-PDG11-SFC6J-D9C15-VFP2R-41N3Z-Z0V8
+11/5/2026 15:20:46;dani.serra@insterrassa.cat;1r BAT B;RC282-C0DEE-C0012-4HW72-BY04C-3ZZ0Z-Z0BR
+11/11/2026 19:45:37;aina.puig@insterrassa.cat;1r BAT A;RC2VJ-M0DMJ-G4037-70CM4-PHY67-6R3M4-33NZQ-1PQV1-PDG21-SFK28-T26JD-9C15V-FP1N4-2R4ZZ-0B7
+11/18/2026 18:20:32;aina.puig@insterrassa.cat;1r BAT A;RC2GM-Q0DVH-64037-70CM4-PHY67-6R3M4-33NZQ-1PQV1-PDG41-SFK28-T31PN-K2V40-6JD9C-15VFP-1N44C-6ZZ00-Z
+11/19/2026 15:10:35;dani.serra@insterrassa.cat;1r BAT B;RC282-C0DEE-C0012-4HW72-BY04C-3ZZ0Z-Z0BR
+11/20/2026 17:30:16;claudia.moya@insterrassa.cat;1r BAT B;RC2SD-Z0DXG-D0053-716D5-MN171-6R24X-VQ48P-04Z52-H5ZZ0-R8
+11/24/2026 22:40:44;bernat.rius@insterrassa.cat;1r BAT A;RC21J-D0E1N-80015-1RKF4-HW11P-NM4HS-K32Q0-4C64Z-4ZZ0R-9
+11/25/2026 20:30:41;aina.puig@insterrassa.cat;1r BAT A;RC2VN-N0E2K-740B7-70CM4-PHY67-6R3M4-33NZQ-1PQV1-PDG41-SFK28-T31PN-K32Q0-60000-00000-00000-00000-01PNJ-6JD9C-15VFP-1N44C-6ZZ06-C
+11/25/2026 23:15:25;bernat.rius@insterrassa.cat;1r BAT A;RC2WY-S0E2N-S0037-1RKF4-HW11P-NM4HX-K34EB-1QY94-M2G21-R4W4G-904Z7-4C6ZZ-0GE
+11/26/2026 23:50:49;bernat.rius@insterrassa.cat;1r BAT A;RC2W8-J0E3P-B0037-1RKF4-HW11P-NM4HS-K34EB-1QY94-M2G51-R4W4J-734J2-K3C83-3CV04-Z94C6-ZZ0XJ
+12/2/2026 18:50:22;aina.puig@insterrassa.cat;1r BAT A;RC2PV-W0E9H-N40B7-70CM4-PHY67-6R3M4-33NZQ-1PQV1-PDG41-SFK28-T31PN-K32Q0-70000-00000-00000-00000-01PNK-1PNK6-JD9C1-5VFP1-N54C6-ZZ0TK
+12/3/2026 14:45:17;dani.serra@insterrassa.cat;1r BAT B;RC282-C0DEE-C0012-4HW72-BY04C-3ZZ0Z-Z0BR
+12/4/2026 16:10:43;claudia.moya@insterrassa.cat;1r BAT B;RC256-70EBF-50053-716D5-MN171-6R34X-VQ4V5-348P0-4Z63V-3ZZ0H-6
diff -ru a/js/codi.js b/js/codi.js
--- a/js/codi.js	2026-08-05 12:51:12.268401321 +0000
+++ b/js/codi.js	2026-08-05 12:51:12.269492085 +0000
@@ -16,11 +16,11 @@
 
    ── FORMAT ────────────────────────────────────────────────────────────────
 
-     RC1 SSS DD HH MMM  [ per cada full: G + grups de 4 ]  [ DIAG ] EEEEEEEEE KK
+     RC2 SSS DDD HH MMM  [ per cada full: G + grups de 4 ]  [ DIAG ] EEEEEEEEE KK
 
-     RC1   3   marca i versió
+     RC2   3   marca i versió
      SSS   3   salt aleatori
-     DD    2   dia (dies des de l'1/9/2025)
+     DDD   3   dia (dies des de l'1/9/2025)
      HH    2   hora (minuts/2 des de mitjanit)
      MMM   3   màscara: bits 0-11 = fulls presents, bit 12 = hi ha diagnòstic
      G     1   nombre de grups d'aquest full
@@ -75,6 +75,14 @@
   var ALF = "0123456789ABCDEFGHJKMNPQRSTVWXYZ";
 
   var EPOCA = Date.UTC(2025, 8, 1);           /* 1 de setembre de 2025 */
+
+  /* La data ocupa 3 caràcters (32768 dies, fins al 2115). A la versió RC1
+     n'ocupava 2, i això topava als 1024 dies: a partir del 20 de juny de 2028
+     tots els codis haurien dit la mateixa data, en silenci i sense que res
+     avisés, i l'anàlisi per trimestres hauria quedat inservible. Costava un
+     caràcter arreglar-ho. El lector accepta les dues versions. */
+  var CAR_DIA = { RC1: 2, RC2: 3 };
+  var VERSIO = "RC2";
   var ESTATS = ["", "net", "segon", "pista", "fallat", "vist"];
   var PES = { net: 10, segon: 7, pista: 6, fallat: 0, vist: 0, "": 0 };
 
@@ -133,14 +141,14 @@
     for (var i = 0; i < 3; i++) salt += ALF.charAt(Math.floor(Math.random() * 32));
 
     var dia = Math.floor((Date.UTC(ara.getFullYear(), ara.getMonth(), ara.getDate()) - EPOCA) / 86400000);
-    dia = Math.max(0, Math.min(1023, dia));
+    dia = Math.max(0, Math.min(32767, dia));
     var hora = Math.min(719, Math.floor((ara.getHours() * 60 + ara.getMinutes()) / 2));
 
     var mask = 0;
     fulls.forEach(function (f) { mask |= (1 << (f.n - 1)); });
     if (diag) mask |= (1 << 12);
 
-    var cos = "RC1" + salt + enc(dia, 2) + enc(hora, 2) + enc(mask, 3);
+    var cos = VERSIO + salt + enc(dia, CAR_DIA[VERSIO]) + enc(hora, 2) + enc(mask, 3);
 
     fulls.forEach(function (f) {
       var codis = f.estats.map(function (e) { return Math.max(0, ESTATS.indexOf(e || "")); });
@@ -197,8 +205,9 @@
     var T = window.RE_TAULES;
     var s = neteja(raw);
     if (!s) return { ok: false, error: "Codi buit" };
-    if (s.slice(0, 3) !== "RC1") {
-      return { ok: false, error: "No sembla un codi de repàs-ESO (ha de començar per RC1)" };
+    var versio = s.slice(0, 3);
+    if (!CAR_DIA[versio]) {
+      return { ok: false, error: "No sembla un codi de repàs-ESO (ha de començar per RC2)" };
     }
     if (s.length < 15) return { ok: false, error: "Codi massa curt" };
 
@@ -209,7 +218,7 @@
 
     var p = 3;
     var salt = s.slice(p, p + 3); p += 3;
-    var dia = dec(s.slice(p, p + 2)); p += 2;
+    var dia = dec(s.slice(p, p + CAR_DIA[versio])); p += CAR_DIA[versio];
     var hora = dec(s.slice(p, p + 2)); p += 2;
     var mask = dec(s.slice(p, p + 3)); p += 3;
 
@@ -262,7 +271,7 @@
     }
 
     r = {
-      ok: true, integre: integre, salt: salt,
+      ok: true, integre: integre, salt: salt, versio: versio,
       data: data, hora: Math.floor(minuts / 60), minut: minuts % 60,
       fulls: fulls, diag: diag, errs: errs,
       error: integre ? null : "Els caràcters de control no quadren: el codi s'ha copiat malament o s'ha modificat"
diff -ru a/tools/analitzador-plantilla.html b/tools/analitzador-plantilla.html
--- a/tools/analitzador-plantilla.html	2026-08-05 12:51:12.249294755 +0000
+++ b/tools/analitzador-plantilla.html	2026-08-05 12:51:12.250543938 +0000
@@ -73,6 +73,12 @@
 .n-mitja{background:var(--ambre-suau);color:#6B4200}
 .n-baixa{background:var(--vermell-suau);color:var(--vermell)}
 .ajuda li{margin-bottom:.45rem}
+.camp{border-top:1px solid var(--vora);padding:.7rem 0 .1rem}
+.camp>b{display:block;font:600 11px/1.6 var(--mono);letter-spacing:.05em;
+  text-transform:uppercase;color:var(--apagat);margin-bottom:.4rem}
+.btn.pendent{background:var(--ambre);border-color:var(--ambre)}
+.capçalera-res{background:var(--blau-suau);border:1px solid #CBDCEA;border-radius:10px;
+  padding:.6rem .8rem;font-size:13.5px;margin-bottom:1rem}
 </style>
 </head>
 <body>
@@ -101,7 +107,7 @@
         obrir un CSV. Cal que hi hagi una columna amb el codi; si n'hi ha de correu,
         marca temporal o grup, també les farà servir.
       </p>
-      <textarea id="entrada" placeholder="Marca temporal	Adreça electrònica	Grup	Codi&#10;17/02/2026 10:41:03	alumne@escola.cat	4tA	RC1AM-8599X-…"></textarea>
+      <textarea id="entrada" placeholder="Marca temporal	Adreça electrònica	Grup	Codi&#10;17/02/2026 10:41:03	alumne@escola.cat	4tA	RC2AM-8599X-…"></textarea>
       <div style="display:flex;gap:.5rem;flex-wrap:wrap;margin-top:.7rem;align-items:center">
         <button class="btn" id="btn-llegeix">Llegeix</button>
         <label class="btn buit" style="position:relative;overflow:hidden">
@@ -157,30 +163,45 @@
   <!-- ── PROGRÉS DEL TRIMESTRE ────────────────────────────────────────── -->
   <section id="v-progres" hidden>
     <div class="targeta">
-      <h3>Període i objectius</h3>
-      <p class="petit apagat" style="margin:0 0 .7rem">
+      <h3>Quin període vols avaluar?</h3>
+      <p class="petit apagat" style="margin:0 0 .9rem">
         Els codis són acumulatius, així que la diferència entre dos codis seguits
         és exactament la feina feta entremig. D'aquí surt tot el que hi ha a sota:
         constància, volum i si va millorant.
       </p>
-      <div class="filtres">
-        <label class="petit apagat">Del
-          <input type="date" id="p-ini"></label>
-        <label class="petit apagat">al
-          <input type="date" id="p-fi"></label>
-        <select id="p-preset">
-          <option value="">Trimestre…</option>
-          <option value="1">1r (set–des)</option>
-          <option value="2">2n (gen–març)</option>
-          <option value="3">3r (abril–juny)</option>
-          <option value="tot">Tot el que hi ha</option>
-        </select>
-        <label class="petit apagat">Setmanes esperades
-          <input type="number" id="p-setm" value="10" min="1" max="30" style="width:64px"></label>
-        <label class="petit apagat">Exercicis esperats
-          <input type="number" id="p-exerc" value="60" min="1" max="999" style="width:74px"></label>
-        <label class="petit apagat" title="Els de nivell 3 compten 1,3 i els de nivell 1, 0,8">
-          <input type="checkbox" id="p-dif" checked> pondera per dificultat</label>
+
+      <div class="camp">
+        <b>Període</b>
+        <div class="filtres" style="margin:0">
+          <label class="petit apagat">Del
+            <input type="date" id="p-ini"></label>
+          <label class="petit apagat">al
+            <input type="date" id="p-fi"></label>
+          <select id="p-preset">
+            <option value="">O tria'n un…</option>
+            <option value="1">1r trimestre (set–des)</option>
+            <option value="2">2n trimestre (gen–març)</option>
+            <option value="3">3r trimestre (abril–juny)</option>
+            <option value="tot">Tot el que hi ha al full</option>
+          </select>
+        </div>
+      </div>
+
+      <div class="camp">
+        <b>Quanta feina demanes en aquest període</b>
+        <div class="filtres" style="margin:0">
+          <label class="petit apagat">Setmanes amb feina
+            <input type="number" id="p-setm" value="10" min="1" max="40" style="width:64px"></label>
+          <label class="petit apagat">Exercicis en total
+            <input type="number" id="p-exerc" value="60" min="1" max="999" style="width:74px"></label>
+          <label class="petit apagat" title="Els de nivell 3 compten 1,3 i els de nivell 1, 0,8">
+            <input type="checkbox" id="p-dif" checked> pondera per dificultat</label>
+        </div>
+        <p class="petit apagat" style="margin:.35rem 0 0">
+          Qui arribi a les dues xifres té el màxim d'aquests dos apartats. Les
+          setmanes compten només si porten feina <b>nova</b>: reenviar el mateix
+          codi no suma.
+        </p>
       </div>
       <details style="margin-top:.4rem">
         <summary class="petit apagat" style="cursor:pointer">Pesos de la qualificació</summary>
@@ -203,6 +224,10 @@
           què estàs incentivant.
         </p>
       </details>
+      <div style="display:flex;gap:.6rem;align-items:center;flex-wrap:wrap;margin-top:1rem">
+        <button class="btn" id="p-accepta">Accepta i calcula</button>
+        <span class="petit" id="p-estat"></span>
+      </div>
     </div>
 
     <div id="p-resultats" style="margin-top:1rem"></div>
@@ -212,7 +237,7 @@
   <section id="v-un" hidden>
     <div class="targeta">
       <h3>Enganxa un codi</h3>
-      <input type="text" id="codi-un" placeholder="RC1AM-8599X-00853-…" style="width:100%;font-family:var(--mono)">
+      <input type="text" id="codi-un" placeholder="RC2AM-8599X-00853-…" style="width:100%;font-family:var(--mono)">
       <div style="margin-top:.7rem"><button class="btn" id="btn-un">Llegeix-lo</button></div>
     </div>
     <div id="un-resultat" style="margin-top:1rem"></div>
@@ -291,7 +316,7 @@
       $("#v-full").hidden = b.dataset.mode !== "full";
       $("#v-progres").hidden = b.dataset.mode !== "progres";
       $("#v-un").hidden = b.dataset.mode !== "un";
-      if (b.dataset.mode === "progres") pintaProgres();
+      if (b.dataset.mode === "progres") { proposaPeriode(); pintaProgres(); }
       $("#v-ajuda").hidden = b.dataset.mode !== "ajuda";
     };
   });
@@ -313,7 +338,7 @@
      columna es digui de cap manera concreta: si les capçaleres no ajuden,
      es busca la columna que conté un codi. */
   function buscaCodi(text) {
-    var m = String(text || "").toUpperCase().match(/RC1[0-9A-Z\-\s]{10,}/);
+    var m = String(text || "").toUpperCase().match(/RC[12][0-9A-Z\-\s]{10,}/);
     return m ? m[0].trim() : null;
   }
 
@@ -357,9 +382,41 @@
     var m = String(ts).match(/(\d{1,2}):(\d{2})/);
     return m ? (+m[1]) * 60 + (+m[2]) : null;
   }
+  /* Google exporta la data amb el format del full, que pot ser d/m/Y (català)
+     o m/d/Y (anglès). Amb dies del 1 al 12 no es poden distingir mirant la
+     data tota sola: "8/5/2026" tant pot ser el 8 de maig com el 5 d'agost.
+
+     Sí que es poden distingir comparant-la amb la data que el codi porta a
+     dins, perquè entre generar-lo i enviar-lo passen minuts, no mesos. Es
+     decideix una vegada per a tot el full i per majoria, que és molt més
+     robust que fila a fila. */
+  var FORMAT_DATA = "dm";
+
+  function detectaFormatData(brut) {
+    var dm = 0, md = 0;
+    brut.forEach(function (r) {
+      var p = RE_CODI.llegeix(r.codi);
+      if (!p.ok) return;
+      var m = String(r.enviat).match(/(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})/);
+      if (!m) return;
+      var a = +m[1], b = +m[2], any = +m[3];
+      if (a > 12) { dm++; return; }              /* el primer no pot ser mes */
+      if (b > 12) { md++; return; }
+      var base = p.data.getTime();
+      var v1 = Math.abs(new Date(any, b - 1, a).getTime() - base);   /* d/m/Y */
+      var v2 = Math.abs(new Date(any, a - 1, b).getTime() - base);   /* m/d/Y */
+      if (v1 < v2) dm++; else if (v2 < v1) md++;
+    });
+    return md > dm ? "md" : "dm";
+  }
+
   function dataEnviament(ts) {
     var s = String(ts), m = s.match(/(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})/);
-    if (m) return m[3] + "-" + ("0" + m[2]).slice(-2) + "-" + ("0" + m[1]).slice(-2);
+    if (m) {
+      var dia = FORMAT_DATA === "md" ? m[2] : m[1];
+      var mes = FORMAT_DATA === "md" ? m[1] : m[2];
+      return m[3] + "-" + ("0" + mes).slice(-2) + "-" + ("0" + dia).slice(-2);
+    }
     m = s.match(/(\d{4})-(\d{2})-(\d{2})/);
     return m ? m[0] : "";
   }
@@ -476,6 +533,7 @@
       + dada("Amb pista", p.resum.comptes.pista)
       + dada("Al segon intent", p.resum.comptes.segon)
       + dada("Fallats", p.resum.comptes.fallat)
+      + dada("Oberts i deixats", p.resum.comptes.vist)
       + dada("Per nivell", "1r " + p.resum.perDif[1] + " · 2n " + p.resum.perDif[2] + " · 3r " + p.resum.perDif[3])
       + dada("Codi generat", r.dataGen + " " + ("0" + p.hora).slice(-2) + ":" + ("0" + p.minut).slice(-2))
       + "</div>";
@@ -649,9 +707,10 @@
     var brut = llegeixFull(txt);
     if (!brut.length) {
       $("#estat-carrega").innerHTML = '<span class="mal">No hi he trobat cap codi. '
-        + "Els codis comencen per RC1.</span>";
+        + "Els codis comencen per RC2 (o RC1, els antics).</span>";
       return;
     }
+    FORMAT_DATA = detectaFormatData(brut);
     files = processa(brut);
     obert = null;
     var sel = $("#f-full");
@@ -660,11 +719,16 @@
     vistos.sort(function (a, b) { return a - b; });
     sel.innerHTML = '<option value="">Tots els fulls</option>'
       + vistos.map(function (n) { return '<option value="' + n + '">Full ' + n + "</option>"; }).join("");
-    $("#estat-carrega").innerHTML = '<span class="ok">' + files.length + " codis llegits</span>";
+    $("#estat-carrega").innerHTML = '<span class="ok">' + files.length + " codis llegits</span>"
+      + ' <span class="apagat petit">· dates en format '
+      + (FORMAT_DATA === "md" ? "mes/dia/any" : "dia/mes/any") + "</span>";
     $("#resultats").hidden = false;
     obertP = null;
+    cfgAplicada = null;
+    $("#p-accepta").classList.remove("pendent");
+    $("#p-estat").textContent = "";
     pinta();
-    if (!$("#v-progres").hidden) pintaProgres();
+    if (!$("#v-progres").hidden) { proposaPeriode(); pintaProgres(); }
   }
 
   $("#btn-llegeix").onclick = function () { carrega($("#entrada").value); };
@@ -681,9 +745,26 @@
 
   ["#p-ini", "#p-fi", "#p-setm", "#p-exerc", "#p-dif",
    "#p-w1", "#p-w2", "#p-w3", "#p-w4"].forEach(function (s) {
-    $(s).oninput = function () { obertP = null; pintaProgres(); };
-    $(s).onchange = function () { obertP = null; pintaProgres(); };
+    $(s).oninput = marcaPendent;
+    $(s).onchange = marcaPendent;
   });
+  $("#p-accepta").onclick = aplicaProgres;
+
+  /* En obrir la pestanya per primera vegada, s'omplen les dates amb el que
+     cobreixen les dades. És una proposta, no una decisió: fins que no es prem
+     el botó no es calcula res. */
+  function proposaPeriode() {
+    if (cfgAplicada || $("#p-ini").value || !files.length) return;
+    var ds = files.filter(function (r) { return r.p.ok; }).map(function (r) { return quan(r.p); });
+    if (!ds.length) return;
+    var mn = new Date(Math.min.apply(null, ds)), mx = new Date(Math.max.apply(null, ds));
+    $("#p-ini").value = mn.toISOString().slice(0, 10);
+    $("#p-fi").value = mx.toISOString().slice(0, 10);
+    $("#p-setm").value = Math.max(1, Math.round((mx - mn) / 604800000) + 1);
+    $("#p-estat").innerHTML = '<span class="apagat">Hi ha codis del '
+      + mn.toISOString().slice(0, 10) + " al " + mx.toISOString().slice(0, 10)
+      + ". Ajusta el que calgui i prem <b>Accepta i calcula</b>.</span>";
+  }
 
   /* Els trimestres es dedueixen de l'any que tenen les dades, no d'un any
      fix: així el fitxer segueix servint el curs vinent. */
@@ -705,7 +786,11 @@
     } else if (v === "1") { posa((any - 1) + "-09-01", (any - 1) + "-12-22", 14); }
     else if (v === "2") { posa(any + "-01-08", any + "-03-31", 11); }
     else { posa(any + "-04-01", any + "-06-20", 11); }
-    obertP = null; pintaProgres();
+    marcaPendent();
+    if (!cfgAplicada) {
+      $("#p-estat").innerHTML = '<span class="apagat">Període posat. Ara prem '
+        + "<b>Accepta i calcula</b>.</span>";
+    }
   };
 
   $("#btn-csv").onclick = function () {
@@ -842,17 +927,31 @@
       });
       /* La feina d'abans del trimestre no compta: es parteix de l'últim codi
          anterior al període com a línia de base. */
-      var prev = abans.length ? abans[abans.length - 1].mapa : {};
+      var prev = {};
+      if (abans.length) Object.keys(abans[abans.length - 1].mapa).forEach(function (id) {
+        prev[id] = abans[abans.length - 1].mapa[id];
+      });
 
+      /* La línia de base és ACUMULATIVA: la unió de tot el que s'ha vist fins
+         ara, no l'últim codi tot sol. Normalment és el mateix, perquè cada
+         codi conté l'anterior. Deixa de ser-ho si l'alumne perd les dades del
+         navegador (les esborra, canvia de dispositiu, finestra privada) i
+         torna a començar: llavors el seu codi següent porta MENYS exercicis
+         que el previ. Amb una línia de base acumulativa, allò que repeteix no
+         es torna a comptar; amb l'últim codi tot sol, sí, i el volum del
+         trimestre sortia inflat. */
+      var perduda = [];
       var passos = [];
       dins.forEach(function (c) {
-        var nous = [], repetits = [];
+        var nous = [], repetits = [], vistos = Object.keys(c.mapa).length;
+        var previs = Object.keys(prev).length;
         Object.keys(c.mapa).forEach(function (id) {
           if (!prev[id]) nous.push(c.mapa[id]);
           else if (prev[id].e !== c.mapa[id].e) repetits.push({ e: c.mapa[id].e, d: c.mapa[id].d, abans: prev[id].e });
         });
-        passos.push({ quan: c.quan, nous: nous, repetits: repetits, codi: c.r, total: Object.keys(c.mapa).length });
-        prev = c.mapa;
+        if (previs && vistos < previs) perduda.push({ quan: c.quan, abans: previs, ara: vistos });
+        passos.push({ quan: c.quan, nous: nous, repetits: repetits, codi: c.r, total: vistos });
+        Object.keys(c.mapa).forEach(function (id) { prev[id] = c.mapa[id]; });
       });
 
       /* Un enviament només compta com a dia de feina si porta exercicis nous.
@@ -915,7 +1014,7 @@
         dies: nDies, setmanes: nSetm, passos: ambFeina,
         exercicis: glob.n, volum: volum, glob: glob, A: A, B: B,
         dEncert: dEncert, dPistes: dPistes, mesurable: mesurable, repetits: repetits,
-        comp: comp, nota: nota,
+        comp: comp, nota: nota, perduda: perduda,
         primer: dins.length ? dins[0].quan : null,
         ultim: dins.length ? dins[dins.length - 1].quan : null
       };
@@ -934,19 +1033,90 @@
       Math.round(100 * Math.min(1, v)) + "%" + (color ? ";background:" + color : "") + '"></i></span>';
   }
 
+  /* Les qualificacions NO es recalculen a cada tecla: es recalculen quan es
+     prem "Accepta i calcula". Posar una nota a algú no és una previsualització,
+     i veure les xifres ballar mentre s'escriu un any convida a mirar-se-les
+     abans que estiguin acabades. Mentre hi hagi canvis sense aplicar, el botó
+     es posa ambre i ho diu. */
+  var cfgAplicada = null;
+
+  function marcaPendent() {
+    if (!cfgAplicada) return;
+    $("#p-accepta").classList.add("pendent");
+    $("#p-estat").innerHTML = '<span class="avis">Has canviat alguna cosa: '
+      + "prem <b>Accepta i calcula</b> per tornar a fer els números.</span>";
+  }
+
+  function dataDeText(v) { return v ? new Date(v + "T00:00:00") : null; }
+
+  function aplicaProgres() {
+    if (!files.length) {
+      $("#p-estat").innerHTML = '<span class="mal">Primer has de llegir un full '
+        + "de respostes a la primera pestanya.</span>";
+      return;
+    }
+    var i = dataDeText($("#p-ini").value), f = dataDeText($("#p-fi").value);
+    if (!i || !f) {
+      $("#p-estat").innerHTML = '<span class="mal">Falta dir de quina data a '
+        + "quina. Pots triar un trimestre de la llista.</span>";
+      return;
+    }
+    if (i > f) {
+      $("#p-estat").innerHTML = '<span class="mal">La data d\'inici és posterior '
+        + "a la de final.</span>";
+      return;
+    }
+    cfgAplicada = cfgProgres();
+    $("#p-accepta").classList.remove("pendent");
+    $("#p-estat").textContent = "";
+    obertP = null;
+    pintaProgres();
+  }
+
   function pintaProgres() {
     if (!files.length) {
-      $("#p-resultats").innerHTML = '<div class="buit">Primer llegeix un full de respostes.</div>';
+      $("#p-resultats").innerHTML = '<div class="buit">Primer llegeix un full de respostes '
+        + "a la pestanya <b>Full de respostes</b>.</div>";
       return;
     }
-    var cfg = cfgProgres();
+    if (!cfgAplicada) {
+      $("#p-resultats").innerHTML = '<div class="buit">Digues quin període vols avaluar '
+        + "i quanta feina hi demanes, i prem <b>Accepta i calcula</b>.</div>";
+      return;
+    }
+    var cfg = cfgAplicada;
     var res = analitzaTrimestre(files, cfg);
     if (!res.length) {
-      $("#p-resultats").innerHTML = '<div class="buit">Cap alumne amb codis vàlids en aquest període.</div>';
+      $("#p-resultats").innerHTML = '<div class="buit">Cap codi vàlid al full.</div>';
+      return;
+    }
+    /* Qui té codis al full però cap dins del període hi surt igualment, amb
+       un zero: no haver fet res durant el trimestre és una nota, no una
+       absència de dades. Ara bé, si això li passa a TOTHOM, el més probable
+       no és que la classe hagi plegat sinó que les dates estiguin malament. */
+    if (res.every(function (a) { return !a.enviaments; })) {
+      var ds = files.filter(function (r) { return r.p.ok; }).map(function (r) { return quan(r.p); });
+      var mn = new Date(Math.min.apply(null, ds)), mx = new Date(Math.max.apply(null, ds));
+      $("#p-resultats").innerHTML = '<div class="buit">Cap alumne no ha enviat res '
+        + "entre aquestes dues dates.<br><span class=\"petit\">Els codis del full van "
+        + "del <b>" + mn.toISOString().slice(0, 10) + "</b> al <b>"
+        + mx.toISOString().slice(0, 10) + "</b>.</span></div>";
       return;
     }
 
-    var h = '<h2>Qualificació del període</h2>';
+    /* Que sempre quedi escrit a quines condicions correspon el que es veu:
+       una nota sense el període i els objectius al costat no vol dir res. */
+    var dt = function (d) {
+      return ("0" + d.getDate()).slice(-2) + "/" + ("0" + (d.getMonth() + 1)).slice(-2)
+        + "/" + d.getFullYear();
+    };
+    var h = '<div class="capçalera-res"><b>' + dt(cfg.ini) + " – " + dt(cfg.fi) + "</b>"
+      + '<span class="apagat"> · es demanen </span><b>' + cfg.setmanes + " setmanes</b>"
+      + '<span class="apagat"> amb feina i </span><b>' + cfg.exercicis + " exercicis</b>"
+      + (cfg.difPes ? '<span class="apagat">, ponderats per dificultat</span>' : "")
+      + '<span class="apagat"> · pesos ' + cfg.pesos.join("/") + "</span></div>";
+
+    h += '<h2>Qualificació del període</h2>';
     h += '<div class="taula-env"><table id="t-progres"><thead><tr>'
       + "<th>Alumne</th><th>Grup</th><th>Dies</th><th>Setmanes</th><th>Exercicis</th>"
       + "<th>Encert</th><th>Pistes</th><th>Millora</th><th>Nota</th><th></th>"
@@ -954,8 +1124,14 @@
 
     res.forEach(function (a, i) {
       h += '<tr class="clic" data-p="' + i + '">'
-        + "<td>" + a.alumne + '</td><td class="apagat">' + (a.grup || "—") + "</td>"
-        + "<td>" + a.dies + (a.enviaments > a.ambFeina
+        + "<td>" + a.alumne + (a.enviaments ? "" :
+            ' <span class="xip" title="Té codis al full, però cap en aquest període">'
+            + "res en aquest període</span>") + "</td>"
+        + '<td class="apagat">' + (a.grup || "—") + "</td>"
+        + "<td>" + (a.perduda.length
+            ? '<span class="marca avis" title="En algun moment el seu codi va portar menys '
+              + 'exercicis que abans: sembla que va perdre les dades del navegador">!</span> ' : "")
+        + a.dies + (a.enviaments > a.ambFeina
             ? ' <span class="apagat petit" title="' + (a.enviaments - a.ambFeina)
               + ' enviament(s) sense feina nova">(+' + (a.enviaments - a.ambFeina) + ")</span>" : "") + "</td>"
         + "<td>" + a.setmanes + '<span class="apagat"> / ' + cfg.setmanes + "</span> "
@@ -992,6 +1168,14 @@
       + dada("Constància fluixa", pocaConst + " de " + res.length)
       + "</div>";
 
+    var perduts = res.filter(function (a) { return a.perduda.length; });
+    if (perduts.length) {
+      h += '<p class="petit" style="margin-top:.7rem"><span class="avis marca">!</span> '
+        + "<b>Han perdut el progrés del navegador</b> en algun moment: "
+        + perduts.map(function (a) { return a.alumne; }).join(", ")
+        + ". Convé recordar-los que facin servir sempre el mateix navegador.</p>";
+    }
+
     if (concentrat.length) {
       h += '<p class="petit" style="margin-top:.7rem"><span class="avis marca">!</span> '
         + "<b>Feina concentrada en pocs dies</b> (molt volum, poca constància): "
@@ -1072,6 +1256,19 @@
     });
     h += "</table></div>";
 
+    if (a.perduda.length) {
+      h += '<p class="petit" style="margin-top:.6rem"><span class="marca avis">!</span> '
+        + "<b>Sembla que ha perdut el progrés del navegador.</b> ";
+      a.perduda.forEach(function (x) {
+        h += "El " + x.quan.toISOString().slice(0, 10) + " el seu codi va passar de portar "
+          + x.abans + " exercicis a portar-ne " + x.ara + ". ";
+      });
+      h += "El progrés es desa al navegador: esborrar-ne les dades, canviar de "
+        + "dispositiu o treballar en una finestra privada el fa començar de zero. "
+        + "El que ja tenia comptat no s'ha perdut (els codis anteriors el porten), "
+        + "i el que ha repetit no s'ha tornat a comptar.</p>";
+    }
+
     if (a.enviaments > a.ambFeina) {
       h += '<p class="petit apagat" style="margin-top:.6rem">Hi ha ' + (a.enviaments - a.ambFeina)
         + " enviament(s) que no portaven cap exercici nou: no compten com a dia de feina.</p>";
diff -ru a/tools/fes-exemple.js b/tools/fes-exemple.js
--- a/tools/fes-exemple.js	2026-08-05 12:51:12.250543938 +0000
+++ b/tools/fes-exemple.js	2026-08-05 12:51:12.254913290 +0000
@@ -0,0 +1,159 @@
+/* Genera un full de respostes versemblant per provar l'analitzador.
+   Els codis són reals: els fa el mateix js/codi.js del projecte, i per tant
+   validen, es descodifiquen i es poden analitzar com els de debò.
+
+   Quatre alumnes amb perfils diferents al llarg del 1r trimestre
+   (20/9/2026 – 5/12/2026), pensats perquè cadascun ensenyi una cosa de
+   l'analitzador. Vegeu EXEMPLE-LLEGEIX-ME.md.
+
+   ús:  node tools/fes-exemple.js > exemple-respostes.csv
+*/
+const fs = require("fs"), path = require("path");
+const ARREL = path.join(__dirname, "..");
+global.window = global;
+eval(fs.readFileSync(path.join(ARREL, "js/codi-taules.js"), "utf8"));
+eval(fs.readFileSync(path.join(ARREL, "js/codi.js"), "utf8"));
+const T = window.RE_TAULES, RE = window.RE_CODI;
+
+/* Generador pseudoaleatori amb llavor, perquè el fitxer surti sempre igual. */
+let llavor = 20260920;
+const rnd = () => { llavor = (llavor * 1103515245 + 12345) & 0x7fffffff; return llavor / 0x7fffffff; };
+const tria = a => a[Math.floor(rnd() * a.length)];
+
+/* Cua d'exercicis, bloc a bloc. Es demana per blocs i no per fulls sencers
+   perquè un alumne real no es fa els 140 exercicis del Full 1 abans de tocar
+   el Full 2: va picant d'aquí i d'allà segons el que li toca repassar. */
+function cua(blocs) {
+  const q = [];
+  blocs.forEach(([n, id]) => {
+    const t = T.fulls[n], b = t.blocs.filter(x => x[0].toLowerCase().indexOf(id) >= 0)[0];
+    if (!b) throw new Error("bloc no trobat: " + n + "/" + id);
+    for (let i = b[1]; i <= b[2]; i++) q.push({ n, id: t.items[i], d: +t.dif[i] });
+  });
+  return q;
+}
+
+/* Un alumne va acumulant estat; cada sessió n'hi afegeix un tros. */
+function Alumne(correu, grup, fulls) {
+  this.correu = correu; this.grup = grup;
+  this.cua = cua(fulls); this.i = 0; this.estat = {}; this.files = [];
+}
+Alumne.prototype.sessio = function (quan, quants, mescla, errs, diag) {
+  for (let k = 0; k < quants && this.i < this.cua.length; k++) {
+    const x = this.cua[this.i++];
+    this.estat[x.n + ":" + x.id] = tria(mescla);
+  }
+  this.emet(quan, errs, diag);
+};
+Alumne.prototype.emet = function (quan, errs, diag) {
+  const perFull = {};
+  Object.keys(this.estat).forEach(k => {
+    const [n, id] = k.split(":"); (perFull[n] = perFull[n] || {})[id] = this.estat[k];
+  });
+  const fulls = Object.keys(perFull).map(n =>
+    ({ n: +n, estats: T.fulls[n].items.map(id => perFull[n][id] || "") }));
+  const codi = RE.genera({ fulls, ara: quan, errs: errs || [], diag: diag || null });
+  this.files.push({ quan, codi });
+  return codi;
+};
+Alumne.prototype.reenvia = function (quan) {
+  this.files.push({ quan, codi: this.files[this.files.length - 1].codi });
+};
+
+const D = (mes, dia, h, m) => new Date(2026, mes - 1, dia, h, m, 0);
+
+// ── mescles d'estats, de pitjor a millor ────────────────────────────────
+const FLUIX = ["fallat", "pista", "pista", "segon", "net", "fallat", "vist"];
+const MITJA = ["pista", "segon", "net", "net", "fallat", "segon"];
+const BO = ["net", "net", "net", "segon", "pista"];
+const MOLTBO = ["net", "net", "net", "net", "segon"];
+
+const alumnes = [];
+
+// ── AINA: constant i millora clarament. Fa el test inicial. ─────────────
+{
+  const a = new Alumne("aina.puig@insterrassa.cat", "1r BAT A",
+    [[1, "enters"], [1, "divisibilitat"], [2, "càlcul de potències"],
+     [4, "igualtats notables"], [4, "factor comú"]]);
+  const diag = T.proves.map((p, i) => ({ estat: i < 5 ? 1 : (i < 10 ? 2 : 3), encert: i % 3 === 0 }));
+  a.sessio(D(9, 22, 18, 40), 5, FLUIX, [["SIGNE_FINAL", 3]], diag);
+  a.sessio(D(9, 29, 19, 10), 7, FLUIX, [["SIGNE_FINAL", 4], ["PARENTESI_NO_DISTRIBUIT", 2]], diag);
+  a.sessio(D(10, 6, 18, 55), 6, MITJA, [["SIGNE_FINAL", 5], ["TRACTAT_COM_EXACTE", 2]], diag);
+  a.sessio(D(10, 14, 20, 5), 8, MITJA, [["TRACTAT_COM_EXACTE", 4], ["SIGNE_FINAL", 5]], diag);
+  a.sessio(D(10, 21, 17, 30), 7, MITJA, [["TRACTAT_COM_EXACTE", 5], ["NO_RESTA_ANTEPERIODE", 3]], diag);
+  a.sessio(D(10, 28, 19, 25), 8, BO, [["NO_RESTA_ANTEPERIODE", 4], ["TRACTAT_COM_EXACTE", 5]], diag);
+  a.sessio(D(11, 4, 18, 15), 9, BO, [["NO_RESTA_ANTEPERIODE", 4], ["EXPONENT_COM_PRODUCTE", 3]], diag);
+  a.sessio(D(11, 11, 19, 45), 8, BO, [["EXPONENT_COM_PRODUCTE", 4], ["NO_RESTA_ANTEPERIODE", 4]], diag);
+  a.sessio(D(11, 18, 18, 20), 9, MOLTBO, [["EXPONENT_COM_PRODUCTE", 4], ["SIGNE_FINAL", 6]], diag);
+  a.sessio(D(11, 25, 20, 30), 7, MOLTBO, [["EXPONENT_COM_PRODUCTE", 4], ["SIGNE_FINAL", 6]], diag);
+  a.sessio(D(12, 2, 18, 50), 8, MOLTBO, [["EXPONENT_COM_PRODUCTE", 5], ["SIGNE_FINAL", 6]], diag);
+  alumnes.push(a);
+}
+
+// ── BERNAT: no fa res fins al final i llavors s'ho empassa tot de cop ───
+{
+  const b = new Alumne("bernat.rius@insterrassa.cat", "1r BAT A",
+    [[1, "enters"], [1, "divisibilitat"], [2, "càlcul de potències"],
+     [2, "exponent negatiu"]]);
+  b.sessio(D(10, 2, 12, 10), 4, MITJA, [["SIGNE_FINAL", 2]]);
+  b.sessio(D(11, 24, 22, 40), 26, BO, [["SIGNE_FINAL", 6], ["TRACTAT_COM_EXACTE", 4]]);
+  b.sessio(D(11, 25, 23, 15), 24, BO, [["TRACTAT_COM_EXACTE", 7], ["SIGNE_FINAL", 6]]);
+  b.sessio(D(11, 26, 23, 50), 22, BO, [["TRACTAT_COM_EXACTE", 9], ["SIGNE_FINAL", 6]]);
+  alumnes.push(b);
+}
+
+// ── CLÀUDIA: irregular, li costa i no acaba d'arrencar ─────────────────
+{
+  const c = new Alumne("claudia.moya@insterrassa.cat", "1r BAT B",
+    [[1, "enters"], [3, "termes d'una successió"], [3, "progressions aritmètiques"]]);
+  c.sessio(D(9, 24, 16, 20), 8, FLUIX, [["SIGNE_FINAL", 4], ["MCD_EN_LLOC_DE_MCM", 2]]);
+  c.sessio(D(10, 9, 17, 5), 6, FLUIX, [["MCD_EN_LLOC_DE_MCM", 4], ["SIGNE_FINAL", 5]]);
+  c.sessio(D(10, 30, 16, 45), 7, FLUIX, [["MCD_EN_LLOC_DE_MCM", 5], ["TRACTAT_COM_EXACTE", 3]]);
+  c.sessio(D(11, 20, 17, 30), 6, FLUIX, [["TRACTAT_COM_EXACTE", 5], ["MCD_EN_LLOC_DE_MCM", 5]]);
+  c.sessio(D(12, 4, 16, 10), 7, MITJA, [["TRACTAT_COM_EXACTE", 6], ["PROGRESSIO_INVENTADA", 3]]);
+  alumnes.push(c);
+}
+
+// ── DANI: fa poc i reenvia el mateix codi per semblar actiu ─────────────
+{
+  const d = new Alumne("dani.serra@insterrassa.cat", "1r BAT B",
+    [[1, "enters"], [1, "divisibilitat"]]);
+  d.sessio(D(9, 25, 15, 30), 6, BO, [["SIGNE_FINAL", 2]]);
+  d.reenvia(D(10, 8, 15, 40));
+  d.reenvia(D(10, 22, 14, 55));
+  d.sessio(D(11, 5, 15, 20), 3, MITJA, [["SIGNE_FINAL", 3]]);
+  d.reenvia(D(11, 19, 15, 10));
+  d.reenvia(D(12, 3, 14, 45));
+  alumnes.push(d);
+}
+
+// ── el full ─────────────────────────────────────────────────────────────
+const files = [];
+alumnes.forEach(a => a.files.forEach(f => files.push({ a, f })));
+
+/* Dos retocs perquè el full sembli de debò i es vegin els avisos:
+   - un enviament fet hores després de generar el codi (avís de temps)
+   - un codi mal copiat, amb un caràcter canviat (control trencat) */
+const tard = files.filter(x => x.a.correu.startsWith("claudia"))[2];
+tard.retard = 190;
+const trencat = files.filter(x => x.a.correu.startsWith("bernat"))[2];
+trencat.f = { quan: trencat.f.quan, codi: trencat.f.codi.slice(0, 34) + "X" + trencat.f.codi.slice(35) };
+
+files.sort((x, y) => x.f.quan - y.f.quan);
+
+/* El Google Form del centre desa la marca de temps en mes/dia/any, que és
+   el que fa el full de l'exemple real. L'analitzador ho detecta sol
+   comparant-ho amb la data que porta el codi a dins. */
+function marca(d, retard) {
+  const t = new Date(d.getTime() + (retard || 0) * 60000 + Math.floor(rnd() * 50) * 1000);
+  const p = n => ("0" + n).slice(-2);
+  return (t.getMonth() + 1) + "/" + t.getDate() + "/" + t.getFullYear() + " " +
+    p(t.getHours()) + ":" + p(t.getMinutes()) + ":" + p(t.getSeconds());
+}
+
+const linies = ["Marca de temps;Adreça electrònica;Selecciona la classe;" +
+                "Enganxa aquí el codi que has copiat a step-quiz"];
+files.forEach(x => {
+  linies.push([marca(x.f.quan, x.retard), x.a.correu, x.a.grup, x.f.codi].join(";"));
+});
+process.stdout.write("\ufeff" + linies.join("\n") + "\n");
